#include "main.h"

/**
 * _abs - it returns absolute value i.e without sign
 * @i: integer character
 * Return: value of i without sign
 */

int _abs(int i)
{

	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}

